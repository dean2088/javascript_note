/*
Object 遍历 keys
*/
{
	Object.keys(vendorsInfoItem).map( k =>
              <TextField key={k} 
              			 floatingLabelText={vendorsInfoItem[k].label} 
              			 fullWidth={ true } 
              			 value={vendorsInfoItem[k].value} 
              			 onChange={e => this.handleVendorsInfoChange(e,index,k)} />
            )
}

/*
closer
*/
(res,list)=>this.complete(res,list,{somethinglese})

/*
es6 create function
*/
api.events.onForbidden = () => {

}


/*
yield
*/
generator function* yield
P.coroutine(function*(req,res)){

}